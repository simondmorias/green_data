bundle:
  name: green_etl_bundle

workspace:
  host: https://adb-3198878924845631.11.azuredatabricks.net/
  profile: rgm_poc

targets:
  dev:
    default: true
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/green_etl_dev
    
  prod:
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/green_etl_prod

